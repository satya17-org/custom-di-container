# Implementation Tasks: Custom DI Container

- [x] Create project structure and `pom.xml`
- [x] Implement DI Annotations (`@Component`, `@Autowired`, `@PostConstruct`, `@PreDestroy`)
- [x] Implement the DI Container (`CustomApplicationContext`) with scanning and injection
- [x] Create example components (`Car`, `Engine` with lifecycle logging)
- [x] Create main application runner `CustomDiApplication`
- [x] Compile and run the project to verify behavior
